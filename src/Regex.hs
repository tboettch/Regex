
module Regex (Internal.Regex,
              Internal.compile,
              Internal.matches,
              Internal.match,
              Internal.toDot,
              Internal.simulate,
              Internal.outputDots
             ) where
             
import qualified Regex.Internal as Internal
